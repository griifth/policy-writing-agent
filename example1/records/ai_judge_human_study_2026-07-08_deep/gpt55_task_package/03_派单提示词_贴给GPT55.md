# 派单提示词（整段复制，直接贴给 GPT-5.5）

> 用法：把下面分隔线以内的全部文字贴给 GPT-5.5。若它具备"子代理/新会话"能力，一次贴入即可跑完 19 单；若它一次只能当一个评委（无法隔离上下文），它会按提示词里的降级方案改为"叫你逐单粘贴"。

---

你现在是一项盲测实验的**派单执行官**。实验材料在本机：

任务包目录：`/Users/hujingkai/Documents/New project/example1/records/ai_judge_human_study_2026-07-08_deep/gpt55_task_package/`

## 你的工作

1. 先读任务包目录下的 `00_README_执行协议.md`，理解验收规则。
2. 处理 `tasks/J01.md` 到 `tasks/J19.md` 共 19 个评审任务。**每个任务必须由一个互相隔离、无本实验先验信息的全新子代理完成**：为每个任务开一个独立子代理（上下文互不可见），把**该任务文件的全文**作为其唯一指令，让它照办（任务文件内已含标尺路径、材料路径、输出路径、输出模板）。19 单之间没有依赖关系，允许并行派发；重派的任务同样必须是全新子代理。
3. 每单完成后你做验收（验收时你只准打开 `results/` 下该结果文件本身）：
   - `results/JXX.md` 存在；
   - 文件末尾有 ```json 围栏块，且能被 `python3 -c "import json,sys;json.loads(sys.stdin.read())"` 解析通过（这是你唯一允许运行的命令）；
   - JSON 的 `docs`/`pairs` 与 `source_guess` 覆盖了该任务的全部稿件；
   - 报告里的理由带有原文引文。
   - 不合格 → 作废该结果文件（改名加后缀 `.void`），**开一个全新执行者重做**，不许让原执行者修补。
4. 19 单全部验收通过后，再派 `tasks/W2_01_应试写手.md`（同样一个全新执行者）。
5. 全部完成后，在 `results/` 下写一个 `_派单完成清单.md`：每单的完成时间、是否一次通过、重派次数。

## 铁律（你和所有执行者都要遵守）

- 除了 `00_README_执行协议.md`、`tasks/` 下的任务文件、以及验收时的 `results/` 结果文件，你**一个多余的文件都不准读**。
- 绝对禁止读取路径中含 `_label_map`、`_normalize`、`agent_notes`、`工作日志`、`实验设计`、`最终结论`、`评审割裂`、`人类稿优势`、`分析计划` 字样的任何文件——读了整场实验作废。
- 各评审任务的执行者只准读它自己任务文件里列出的路径。
- 你自己不要去读 `blind_v2/` 下的任何稿件——你是派单员，不是评委。
- 不修改任务包里的任何既有文件；你只往 `results/` 写东西。
- 禁止联网检索稿件内容。

现在开始执行，直到 19 单全部验收通过、W2 完成、`_派单完成清单.md` 落盘为止。
