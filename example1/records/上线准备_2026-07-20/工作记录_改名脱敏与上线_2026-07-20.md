# 工作记录：direction_workflow 改名脱敏＋依赖入包＋上线 GitHub main（2026-07-20）

> 执行人：Claude（领班模式）。用户决策：新名用推荐组；SKILL 内容与 references 一并脱敏，代称统一「某研究员」；蒸馏材料按「不影响写作能力则不上传」处置；补依赖文档与 requirements；整理后的包上传 GitHub main、旧 main 内容替换；改名不得影响使用；完工后派 agent 干跑模拟（不真跑）。

---

## 一、蒸馏材料影响判定（用户问题的答案）

核对两个顾问 skill 的运行时读取规则后判定：

| 材料 | 运行影响 | 处置 |
|---|---|---|
| `references/纲要地图.md`（strategy-preread） | **必需**——SKILL 规定"每次涉及政策挂接必读" | **保留随包** |
| `references/蒸馏报告.md`、`会议证据.md`（两个 skill）、CNKI 检索 csv/log | **不影响写作**——仅"需要追溯结论依据/核对会议原话"时才读，且 SKILL 明文"普通调用不要整份加载" | **移出包**（存 git 历史与内部仓库），SKILL 中的引用行同步改为"溯源材料不随包分发"声明 |

## 二、改名与脱敏（全部完成）

- **目录改名**：`skills/yongjun-thinking/ → skills/topic-ideation/`（定题立论顾问）；`skills/cao-thinking/ → skills/strategy-preread/`（结构预读顾问）。
- **SKILL 本体**：frontmatter name/description 及正文真名全部代称化（张永军/曹培杰/曹所长/张老师 → 某研究员/该研究员）；两个 `agents/openai.yaml` 的 display_name 与 `$skill名` 同步。
- **图纸（按维护纪律：先真源后快照）**：
  - 真源 `records/装配式图纸_2026-07-12/调度宪法_SKILL草案.md` 第 6 条名称映射＋文末《改名备注》（定性：名称映射、非规则变更，零改动纪录延续）；
  - 真源 `元模块契约.md` 3 处名称映射＋文末改名备注；
  - `blueprints/` 三快照（宪法/契约/元框架v2）刷新为 2026-07-20 版，头注注明"对外发布版人名已代称化，内部真源保留原名"。
- **包内引用**：装配工 SKILL（8 处，含宪法八条照录段）、预读提词单模板（口吻标签）、观点清单/方向书模板（"某研究员基准"）、纲要地图头注、lexicon 词表来源注、README/USAGE/移植说明——全部替换。
- **不改区（历史留痕原则）**：`runs/` 与 `_skill_snapshots/` 原样保留在内部分支（真名为当时实况），**不上发布包**。
- **验证**：全包 grep（排除两个历史目录）真名（张永军/曹培杰/张老师/曹所长/永军/曹）**零命中**；旧 skill 名仅剩 3 处**刻意保留的改名备注**（README:91、宪法:61、契约:228，不含真名）。

## 三、依赖入包（全部完成）

- 新增 [requirements.txt](../../direction_workflow/requirements.txt)：`python-docx`（仅排版工序需要；precheck/engine 纯标准库，已核实）。
- 新增 [依赖说明.md](../../direction_workflow/依赖说明.md)：一页纸讲清四件必备（Python3/python-docx/联网/agent 会话）＋ **.env 位置**（包上一级目录，密钥不入包，`--env`/`DEEPSEEK_ENV_PATH` 可覆盖）＋可选依赖降级面＋部署后自检三条。
- `engine/` 三处文档去掉写死的 `example1` 目录名，统一为"包的上一级目录"（代码本身是相对定位，未动逻辑）。
- 移植说明：§一补 pip 步骤、§三快照表补元框架v2 行、§四第 3/4/5 条更新（蒸馏材料不随包；neican_docx 已入包不再是"未接入"）。

## 四、模拟测试（agent 干跑，未真跑）

派全新 agent 以「刚拿到包的装配工」视角走完 14 步（触点由其模拟点选，禁联网/禁 DeepSeek/只读不写；机械件真跑）。**总判定：能走通，零阻断。**

- 14 步所需图纸/模板/skill/规程/词表/脚本全部在包、相互指认一致；
- precheck.py 对历史成稿实跑出七项读数、词表 22+36 条正常载入；python-docx 导入 OK；run_entity.py --help OK；
- 改名残留、被移除材料的引用残留：均零（历史目录除外，符合预期）；
- 查出 4 条降级级文档漂移（均为 7-15 v2 迭代漏改的「用途类型→问题判型」旧描述＋移植说明一条过时条目，**非本次改名造成**）——已全部当场修复：report-clarify-v2 SKILL 6 处、clarify_scope 2 处、闸门规程 2 处、清稿规程 1 处、移植说明 1 处。

## 五、Git 与上线

| 动作 | 结果 |
|---|---|
| 旧 main 备份 | 本地分支 `backup/main-20260720`（= 旧 main `d39aec5`），可随时找回 |
| 内部分支提交 | `fix/dna-governance` → `6dba912`（全部改造＋上线准备方案两份） |
| main 发布包构建 | 包内容平铺仓库根；**排除**：runs/ 历史单（仅留 runs/README.md）、_skill_snapshots/、两个可视化 HTML、.DS_Store/__pycache__；共 **70 个文件、952K**；推送前终验真名零残留 |
| 推送 | 孤儿提交 `0a7bb03` 强推 `origin/main`（github.com/griifth/policy-writing-agent），旧内容已替换 |

**main 发布包目录**：README.md（新写，面向发布）｜USAGE.md｜移植说明.md｜依赖说明.md｜requirements.txt｜_问题反馈录.md｜blueprints/（四图纸快照）｜skills/（装配工＋两顾问＋clarify＋search）｜templates/（七模板）｜gates/（闸+两词表）｜finishing/（三规程＋瘦身聚拢内核＋试点档案，已核真名零）｜engine/｜tools/｜runs/（空，仅形制说明）。

## 六、遗留事项（须知）

1. ~~本机 `~/.claude/skills/` 仍是旧名安装件~~ →【2026-07-20 已处置】本机安装件已切换新名：旧件（cao-thinking/yongjun-thinking/旧装配工）归档至 `~/.claude/retired-skills-20260720/`（未删除，可回退）；新件（topic-ideation/strategy-preread/policy-report-assembler）从包内复制安装，装配工 SKILL.md 相对路径已按移植说明替换为绝对路径（图纸指向包内 2026-07-20 刷新快照，与发布版一致；`runs/` 沿旧例保持相对）；23 条绝对路径逐一实测可达，安装件真名/旧名零残留；技能注册表已确认按新名生效。
2. **远端旧分支未脱敏**：`origin/fix/dna-governance` 等分支及其历史仍含真名与历史 runs。若仓库是公开的，任何人可从分支历史看到真名——**彻底对外脱敏需删除远端旧分支或将仓库转私有**，此项需你拍板。
3. 内部 records/ 真源（宪法/契约）已随改名更新；元框架v2 真源保留真名（内部溯源），快照代称化并在头注声明——此差异已记录在快照头注。
4. 两个可视化 HTML（flow_visualization/multi_agent_flow）已提交在内部分支包目录下，未随发布包。
