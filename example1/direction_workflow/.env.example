# ============================================================
# direction_workflow DeepSeek 配置样例（2026-07-22·默认值已对齐生产实测配置）
# 用法：复制本文件为同目录 .env，只需填入密钥即可跑生产单：
#   Mac/Linux:  cp .env.example .env
#   Windows  :  Copy-Item .env.example .env
# .env 已被包级 .gitignore 排除，不会被 git 提交。
# 仅生产单（立论/组稿走 DeepSeek）需要；试跑单 agent 直写不需要本文件。
# 读取优先级：进程环境变量 > .env；.env 位置：包根优先，包上一级兼容。
# ============================================================

# ---- 必填（唯一必须动的行）----
# DeepSeek 开放平台 API 密钥（https://platform.deepseek.com 获取）
DEEPSEEK_API_KEY=

# ---- 可选：以下默认值即本项目生产实测配置，不填=用默认，改动才取消注释 ----
# 模型（默认 deepseek-v4-pro，历史单实测同款）
# DEEPSEEK_MODEL=deepseek-v4-pro

# API 地址（默认官方；用兼容代理时才改）
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# 单次调用超时秒数（默认 600，覆盖长文组稿）
# DEEPSEEK_TIMEOUT_SECONDS=600

# 单次回复 token 上限（默认 12000）
# DEEPSEEK_MAX_TOKENS=12000

# 推理力度 max|high|medium|low（默认 max；引擎失败重试自动逐级降档）
# DEEPSEEK_REASONING_EFFORT=max

# 思考开关 true|false（默认 true；空响应降级重试时引擎自动关思考保正文）
# DEEPSEEK_THINKING_ENABLED=true

# ---- 进程级变量（不写在 .env，需要时 shell 里 export/set）----
# DEEPSEEK_ENV_PATH=<另一个 .env 的路径>   指定非默认位置的配置文件
#
# 注：旧引擎的 DEEPSEEK_STORE_REASONING、NOTEBOOKLM_* 等变量本包不读取，无需配置。
